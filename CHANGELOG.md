# Changelog

## 0.2.2

- Add a public hosted-safe MCP snapshot API for transient, non-file settings sources.
- Add deterministic comparison of stored hosted-safe inventory snapshots.
- Preserve the existing CLI and staged-directory contracts while excluding raw commands, arguments, URLs, values, paths, and annotation coordinates from the new hosted projection.
