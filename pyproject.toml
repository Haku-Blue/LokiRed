[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "lokired"
version = "0.2.1"
description = "CLI scanner for AI-agent and MCP configuration risk."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [
    { name = "Haku Blue" },
]
keywords = [
    "ai-agents",
    "devsecops",
    "mcp",
    "security",
    "scanner",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: Information Technology",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Security",
    "Topic :: Software Development :: Quality Assurance",
]
dependencies = []

[project.optional-dependencies]
test = [
    "jsonschema>=4.0",
]
[project.urls]
Homepage = "https://github.com/Haku-Blue/LokiRed"
Repository = "https://github.com/Haku-Blue/LokiRed"
Issues = "https://github.com/Haku-Blue/LokiRed/issues"
Documentation = "https://github.com/Haku-Blue/LokiRed/tree/main/docs"

[project.scripts]
lokired = "lokired:main"

[tool.setuptools]
py-modules = [
    "baseline",
    "classification",
    "config_adapters",
    "fingerprints",
    "git_snapshots",
    "inventory",
    "lokired",
    "policy",
    "reporter",
    "rule_catalog",
    "run_scanner",
    "scanner_api",
    "security_file_scanner",
]
