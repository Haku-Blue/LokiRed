name: LokiRed

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  lokired-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Scan agent and MCP config
        uses: ./
        with:
          scan-path: "."
          policy-path: ".lokired.yml"
          output-format: "text"
          fail-on: "high"
